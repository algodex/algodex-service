/**
  * @typedef {Object} Member
  * @property {string} name The members's name.
  * @property {number} [age] The members's age.
  * @property {string} [job] The member's job.
  * @property {string} [job] The member's job.
  */


/**
 * @typedef {Object} Block
 * @property {number} rnd how the person is called
 */

/**
 * @typedef {Object} BlockJob
 * @property {Block} data how the person is called
 */

/**
 * @typedef {Object} PouchDB
 */
/**
 * @typedef {Object} Redis
 */

module.exports = {};

