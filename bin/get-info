#!/usr/bin/env node

/* USAGE:
 *
 * bin/get-info --walletAddr=<address>
 */

const args = require('minimist')(process.argv.slice(2));

const getInfo = async => {
  const wallet = args.walletAddr;
  const databases = getDatabases();
};

getInfo();
